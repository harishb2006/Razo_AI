# Backend API base. Defaults to http://127.0.0.1:8000/api/v1 if unset.
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Demo only — the backend's X-API-Key on write routes.
VITE_API_KEY=dev-local-key
